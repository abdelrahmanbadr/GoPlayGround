package main

func main() {
	// three_index_slice()
	// array_declaration()
	// array_of_pointers()
	// slice_declaration()
	map_declaration()
	// sub_slice := slice_subset()
	// s := solve_memoryLeak_sub_slice()
	// // this will cause memory leak
	// // because they are pointing to the same array
	// fmt.Println(sub_slice[:cap(sub_slice)])
	// fmt.Println(s[:cap(s)])
	// fmt.Println(remove())
	// var array [2]int
	// foo(&array)
}
